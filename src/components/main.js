export default function Main(Landing, Dashboard, ContinueWatching, AiRecs, FilmDetail, Analytics) {
  return `
        <main>
      ${Landing}
      ${Dashboard}
      ${ContinueWatching}
      ${AiRecs}
      ${FilmDetail}
      ${Analytics}
    </main>
    `;
}
