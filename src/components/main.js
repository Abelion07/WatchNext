export default function Main(Landing, Dashboard, AiRecs, Roulette, Analytics, FilmDetail) {
  return `
        <main>
      ${Landing}
      ${Dashboard}
      ${AiRecs}
      ${Roulette}
      ${Analytics}
      ${FilmDetail}
    </main>
    `;
}
